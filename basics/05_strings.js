const name ="aryan"
const repocount = 50

// console.log(name + repocount) very bad way of combining strings

console.log(`Hello my name is ${name} and my repo count is ${repocount}.`  ) //good way

// some primary type of operation on string  
// length, toUpperCase, charAt(2), indexOf('t'), substring(0,4)(4excluded), slice(-8,4), trim()(used to remove spaces), replace('x', 'y')(replaces x to y), includes(a)(will chekc is a present)